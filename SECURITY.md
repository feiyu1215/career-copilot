# Security & Threat Model — 诚实声明

> 本文档的目的**不是**声称本工具「绝对安全」，而是白纸黑字写清：**能防什么、防不住什么、数据去哪了**。
> 任何使用者都应以「本地辅助工具」看待它，而非「可信黑盒」。

## 0. 运行模型（请先读这段）

- 本工具**运行在你自己的终端/本地环境**，是**指令级、非沙箱**的。
- 它调用 LLM（如可达网关）做简历/求职信的起草与评审，但**所有对外材料的终稿都应由你本人人工复核**。
- 它**不**自动发邮件、不自动改系统配置、不自动把你的数据外传到任何第三方地址。

## 1. 能防什么（确定性护栏，无 LLM，可 CI / 离线）

| 机制 | 文件 | 防什么 |
|------|------|--------|
| JD 零信任扫描 | `scripts/jd_guard.py` | 把 JD 视为**不可信数据**：扫描 4 类注入——① 元指令/角色劫持 ② 行动指令（诱导发送/篡改/伪造）③ 格式/分隔符注入（`<system>`/`[INST]` 等）④ 数据外泄意图。命中后剥离为纯文本，绝不当指令执行。 |
| 简历四条硬契约 | `scripts/drafter_reviewer.py` | 确定性检查：C_R1 不编造、C_R2 不过度声称（Over-Claim 四面镜子）、C_R3 单源未复现数字不进对外简历、C_R4 JD 注入未被执行。任一违规即阻断对外产出。 |
| 提交前 / CI 安全护栏 | `tools/security_guards.py` | 4 类检查：① `.gitignore` 必须含个人数据模式 ② 已跟踪文件不得含 `.env` ③ `.agents/**/package.json` 禁止生命周期脚本/`trustedDependencies`（防 `bun install` 投毒）④ 权限 allowlist（放宽须同 PR 显式加入白名单）。 |

## 2. 防不住什么（诚实声明，请勿过度信任）

- **扫描 ≠ 绝对免疫**：`jd_guard` 是确定性正则匹配，只能拦**已知形态**。攻击者用未见过的措辞、Unicode 混淆、图片/附件承载的注入，可能漏检。结论：JD 永远按「不可信文本」处理，工具输出的人工复核不可省。
- **LLM 语义评审非确定性**：双轨评审里的 LLM 环节可能漏判或误判；确定性契约（第 1 节）是最后一道硬闸，但仅覆盖已建模的风险面。
- **本地即攻击面**：因为运行在你的终端、且 JD/网页是外部输入，理论上恶意内容可尝试诱导工具去做不该做的事。**护栏设计上已禁止自动外发/改系统/外传数据**，但这依赖代码正确性与你的权限配置——不要把 `Bash(*)` 这类全开权限赋给本工具。
- **模板由你自管**：`templates/`（U6）里的 LaTeX 模板是你自己的文件，工具不审计其内容；请自审模板来源，勿使用来历不明的模板（恶意模板可在编译期执行 arbitrary 代码——这是 LaTeX 本身的特性，非本工具缺陷）。

## 3. 个人数据边界

- **绝不入库**：`.env` / `data/` / `*.pdf`（生成简历）/ `draft*.tex`（草稿）/ `templates/`（个人排版）/ `career_log*.json` 均已被 `.gitignore` 覆盖，**不进入版本库**。
- **数据只在本地**：抓取到的 JD、你的画像、生成的简历 PDF 都留在你机器上；工具不内置任何上传逻辑。
- **密钥管理**：API Key 仅存在于 `.env`（本地、被忽略）。不要把密钥粘贴进会被跟踪的 `.py` / 文档 / 提交里——`security_guards.py` 会在提交前拦截已跟踪的 `.env`。

## 4. 已知缓解措施与最佳实践

1. **永远人工复核**对外投递的简历/求职信终稿。工具产出 = 草稿，不是终审。
2. **权限最小**：给本工具的 Agent 框架权限只开必要命令（白名单见 `security_guards.py` 的 `ALLOWED_PERMISSIONS`），不要给 `Bash(*)`。
3. **提交前跑护栏**：`python tools/security_guards.py`，CI 也应跑。
4. **联网 LLM 走可信网关**：密钥与端点由你配置的 `.env` 决定；不要用未知来源的网关。
5. **模板自审**：只用你信任的 LaTeX 模板。

## 5. 报告安全问题

本仓库为本地个人项目，无独立的安全响应渠道（SRC）。如发现漏洞，请直接在本地修复并复核；涉及依赖（如 LaTeX 引擎、LLM 网关）的问题，请参考对应上游的安全公告。

---

*本威胁模型会随功能演进更新。写作原则是「如实说清边界」，而非「替你拍板安全」。*
